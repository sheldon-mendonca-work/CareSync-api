services:
  billing-service:
    image: billing-service:latest
    container_name: billing-service
    restart: unless-stopped

    ports:
      - "4001:4001"
      - "9001:9001"

    # depends_on:
    #   - patient-service-db

    networks:
      - internal

networks:
  internal:
    external: true